# PostgreSQL — use Neon free tier or local Postgres
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/connoisseur_ops"

# Auth
JWT_SECRET="change-me-to-a-long-random-string-in-production"

# Web (apps/web)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Mobile (apps/mobile) — use your machine LAN IP for Expo Go on a physical device
EXPO_PUBLIC_API_URL="http://localhost:3000"
