# Use your Mac LAN IP + API port (not localhost) for Expo Go on a physical device
EXPO_PUBLIC_API_URL=http://192.168.1.2:3001

# Required for pnpm monorepo — keeps Metro entry at apps/mobile
EXPO_NO_METRO_WORKSPACE_ROOT=1
